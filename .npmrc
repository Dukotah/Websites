# smartcrop-sharp declares an outdated sharp peer range (^0.32.5); we run sharp
# 0.34, which it works with fine (basic raw-pixel extraction). legacy-peer-deps
# lets `npm install` resolve at the root without --force. (Vercel builds the
# gallery, not root, so this only affects local/CI generate installs.)
legacy-peer-deps=true
